// Styles
import './styles/styles.css';

// Vanilla ECMAScript App
const heading = document.createElement('h1');
heading.textContent = 'React Concepts';
document.getElementById('root').appendChild(heading);
